# ============================================================
# Historic Artwork Forgery Detection — Deployment dependencies
# Only what prediction/app.py and prediction/predict.py import
# (Streamlit Cloud reads this file). For training/EDA/GAN
# dependencies, see requirements-train.txt.
# Python 3.11 recommended (see runtime.txt)
# Install: pip install -r requirements.txt
# ============================================================

numpy>=1.24,<2.0
pandas>=2.0,<3.0
Pillow>=10.0,<11.0

# Needed to unpickle models/label_encoder.pkl (an sklearn LabelEncoder),
# even though it isn't imported directly in app.py/predict.py.
scikit-learn>=1.3,<2.0

# PyTorch (CNN classifier + Streamlit app)
# NOTE: For GPU support, install torch separately from https://pytorch.org/get-started/locally/
torch>=2.0,<3.0
torchvision>=0.15,<1.0

# Streamlit UI
streamlit>=1.28,<2.0
